Code.require_file "test_helper.exs", __DIR__

defmodule LetterpressTest do
  use ExUnit.Case

  test "add tests later. maybe. Oh give me a break; this is an experimental spike and you can validate it works by running the demo app" do
    assert(true)
  end
end
